import React, { useState, useEffect, useRef } from 'react';
import { 
  Clock, 
  Dumbbell, 
  Plus, 
  BarChart3, 
  BookOpen, 
  ClipboardCheck, 
  Target, 
  ChevronRight,
  Users,
  CheckCircle2,
  Mail,
  Linkedin,
  Instagram
} from 'lucide-react';

/**
 * Custom hook to detect when an element is visible in the viewport.
 */
const useIntersectionObserver = (options) => {
  const [isVisible, setIsVisible] = useState(false);
  const containerRef = useRef(null);

  useEffect(() => {
    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        setIsVisible(true);
        observer.unobserve(entry.target);
      }
    }, options);

    if (containerRef.current) {
      observer.observe(containerRef.current);
    }

    return () => {
      if (containerRef.current) observer.unobserve(containerRef.current);
    };
  }, [options]);

  return [containerRef, isVisible];
};

// --- Sub-components ---

const StatBar = ({ percentage, label, description, isVisible }) => (
  <div className="mb-8">
    <div className="flex justify-between mb-3">
      <span className="text-white font-bold text-lg">{label}</span>
      <span className="text-[#B0B0B0] font-mono">{percentage}%</span>
    </div>
    <div className="w-full bg-[#1a1a1a] rounded-full h-3 mb-3 overflow-hidden border border-[#222]">
      <div 
        className="h-full rounded-full bg-[#6A1E2B] transition-all duration-1000 ease-out"
        style={{ width: isVisible ? `${percentage}%` : '0%' }}
      ></div>
    </div>
    <p className="text-sm text-[#B0B0B0]/70 italic">{description}</p>
  </div>
);

// --- Main App Component ---

export default function App() {
  const [activeTab, setActiveTab] = useState('clinical');
  const [heroRef, heroVisible] = useIntersectionObserver({ threshold: 0.1 });
  const [distinctionRef, distinctionVisible] = useIntersectionObserver({ threshold: 0.1 });
  const [spectrumRef, spectrumVisible] = useIntersectionObserver({ threshold: 0.1 });
  const [protocolRef, protocolVisible] = useIntersectionObserver({ threshold: 0.1 });

  const demographics = {
    clinical: {
      title: "Chronic Disease Management",
      stats: [
        { p: 47, l: "Australians with Chronic Conditions", d: "Nearly half of the population suffers from 1+ chronic conditions." },
        { p: 85, l: "Condition Improvement with Exercise", d: "Exercise is a Tier 1 treatment for Type 2 Diabetes, CVD, and Osteoporosis." }
      ],
      services: ["NDIS Support", "Medicare (EPC)", "DVA Services", "WorkCover"]
    },
    general: {
      title: "General Wellness & Longevity",
      stats: [
        { p: 15, l: "Adults Meeting Activity Guidelines", d: "Most adults fail to meet the required strength training guidelines." },
        { p: 40, l: "Reduced Mortality Risk", d: "Regular resistance training is linked to a 40% reduction in all-cause mortality." }
      ],
      services: ["1:1 Personal Training", "Small Group Coaching", "Body Composition", "Functional Screening"]
    },
    athlete: {
      title: "Athlete Development",
      stats: [
        { p: 55, l: "Injury Risk Reduction", d: "Structured S&C programs reduce sports injury rates by over half." },
        { p: 22, l: "Performance Output Increase", d: "Average power output increase after 12 weeks of periodized training." }
      ],
      services: ["Sport Specific S&C", "Load Management", "Return to Sport Testing", "Lactate Threshold"]
    }
  };

  return (
    <div className="min-h-screen bg-[#0E0E0E] text-white font-sans selection:bg-[#6A1E2B] selection:text-white">
      
      {/* HERO SECTION */}
      <header 
        ref={heroRef}
        className={`relative min-h-[90vh] flex items-center justify-center overflow-hidden border-b border-[#222] transition-all duration-1000 ${heroVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'}`}
      >
        <div className="absolute top-0 right-1/2 w-[60vw] h-[60vw] bg-[#6A1E2B] rounded-full blur-[180px] opacity-10 translate-x-1/2 -translate-y-1/2"></div>
        
        <div className="max-w-5xl mx-auto px-6 relative z-10 text-center">
          <div className="flex items-center justify-center gap-4 mb-8">
            <div className="h-[1px] w-12 bg-[#6A1E2B]"></div>
            <span className="text-[#B0B0B0] tracking-[0.3em] uppercase text-xs md:text-sm font-semibold">Allied Health & Performance</span>
            <div className="h-[1px] w-12 bg-[#6A1E2B]"></div>
          </div>
          
          <h1 className="text-7xl md:text-9xl font-black tracking-tighter leading-[0.9] mb-8 bg-clip-text text-transparent bg-gradient-to-r from-white to-[#B0B0B0]">
            PRJCT: <br /> LEGACY
          </h1>
          
          <p className="text-xl md:text-2xl text-[#B0B0B0] max-w-2xl mx-auto leading-relaxed mb-10">
            Bridging the gap between clinical rehabilitation and high-performance athletics. 
            <span className="block mt-4 text-white font-bold">
              I don’t just get you back on track, <span className="text-[#6A1E2B]">I build bodies that last.</span>
            </span>
          </p>
          
          <div className="flex flex-col md:flex-row gap-4 justify-center">
            <button className="px-10 py-4 bg-[#6A1E2B] text-white font-bold rounded hover:bg-[#6A1E2B]/80 transition-all uppercase tracking-wide text-sm">
              The Difference
            </button>
            <button className="px-10 py-4 border border-[#333] bg-[#1a1a1a] text-white font-bold rounded hover:bg-white hover:text-black transition-all uppercase tracking-wide text-sm">
              Book Consultation
            </button>
          </div>
        </div>
      </header>

      {/* DISTINCTION SECTION */}
      <section 
        ref={distinctionRef}
        className={`bg-[#0a0a0a] py-24 md:py-32 px-6 border-b border-[#222] transition-all duration-1000 ${distinctionVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'}`}
      >
        <div className="max-w-7xl mx-auto">
          <div className="text-center max-w-3xl mx-auto mb-20">
            <h2 className="text-4xl md:text-5xl font-bold mb-6">Physio vs. <span className="text-[#6A1E2B]">Physiology</span></h2>
            <div className="h-1 w-20 bg-[#222] mx-auto mb-6"></div>
            <p className="text-[#B0B0B0] text-lg leading-relaxed">
              Think of Physio as the "Mechanic" fixing the acute breakdown, and EP as the "Engineer" optimizing the machine for the long haul.
            </p>
          </div>

          <div className="grid md:grid-cols-2 gap-8 lg:gap-16">
            {/* Physio Card */}
            <div className="p-8 md:p-10 rounded-2xl border border-[#333] bg-[#111] hover:border-[#444] transition-all">
              <div className="flex items-center gap-5 mb-8 pb-8 border-b border-[#222]">
                <div className="p-4 rounded-xl bg-[#222] text-[#B0B0B0]">
                  <Clock size={32} />
                </div>
                <div>
                  <h3 className="text-3xl font-bold text-white">Physiotherapy</h3>
                  <span className="text-sm text-[#B0B0B0] uppercase tracking-widest">Acute Care</span>
                </div>
              </div>
              <ul className="space-y-4 text-[#B0B0B0]">
                {["Diagnosis of acute injuries", "Hands-on manual therapy", "Immediate pain relief", "Early-stage rehabilitation"].map((item, i) => (
                  <li key={i} className="flex items-center gap-3">
                    <div className="w-1.5 h-1.5 rounded-full bg-[#444]"></div>
                    <span className="text-lg">{item}</span>
                  </li>
                ))}
                <li className="flex items-center gap-3 font-semibold text-white pt-2">
                  <div className="w-1.5 h-1.5 rounded-full bg-white"></div>
                  <span className="text-lg">Goal: Restoring normal function</span>
                </li>
              </ul>
            </div>

            {/* EP Card */}
            <div className="p-8 md:p-10 rounded-2xl border border-[#6A1E2B]/50 bg-gradient-to-b from-[#1a0508] to-[#0e0e0e] relative overflow-hidden">
              <div className="absolute top-0 right-0 w-32 h-32 bg-[#6A1E2B] blur-[80px] opacity-20"></div>
              <div className="flex items-center gap-5 mb-8 pb-8 border-b border-white/10 relative z-10">
                <div className="p-4 rounded-xl bg-[#6A1E2B] text-white">
                  <Dumbbell size={32} />
                </div>
                <div>
                  <h3 className="text-3xl font-bold text-white">Ex. Physiology</h3>
                  <span className="text-sm text-[#B0B0B0] uppercase tracking-widest font-medium">Chronic & Performance</span>
                </div>
              </div>
              <ul className="space-y-4 relative z-10">
                {["Management of chronic conditions", "Exercise as medicine prescription", "Long-term behavior change", "Strength & conditioning for rehab"].map((item, i) => (
                  <li key={i} className="flex items-start gap-3 text-white/90">
                    <CheckCircle2 size={20} className="text-[#6A1E2B] mt-1 shrink-0" />
                    <span className="text-lg">{item}</span>
                  </li>
                ))}
                <li className="flex items-start gap-3 text-white font-bold pt-2">
                  <CheckCircle2 size={20} className="text-[#6A1E2B] mt-1 shrink-0" />
                  <span className="text-lg">Goal: Optimizing performance & capacity</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </section>

      {/* SPECTRUM SECTION */}
      <section 
        ref={spectrumRef}
        className={`py-24 md:py-32 px-6 max-w-7xl mx-auto transition-all duration-1000 ${spectrumVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'}`}
      >
        <div className="grid lg:grid-cols-12 gap-16">
          <div className="lg:col-span-5">
            <h2 className="text-4xl font-bold mb-6">The Spectrum of <span className="text-[#6A1E2B]">Care</span></h2>
            <p className="text-[#B0B0B0] mb-10 text-lg">
              PRJCT: Legacy operates across the entire health continuum. From clinical pathology to elite athletes.
            </p>
            <div className="flex flex-col gap-3">
              {[
                { id: 'clinical', label: 'Clinical Population', icon: <Plus size={24} /> },
                { id: 'general', label: 'Healthy Population', icon: <Users size={24} /> },
                { id: 'athlete', label: 'Elite Performance', icon: <BarChart3 size={24} /> }
              ].map((tab) => (
                <button
                  key={tab.id}
                  onClick={() => setActiveTab(tab.id)}
                  className={`p-6 text-left rounded-xl border transition-all flex items-center justify-between font-bold text-xl group ${
                    activeTab === tab.id 
                    ? 'border-[#6A1E2B] bg-[#6A1E2B]/10 text-white' 
                    : 'border-transparent bg-[#111] text-[#666] hover:text-white hover:bg-[#1a1a1a]'
                  }`}
                >
                  <div className="flex items-center gap-4">
                    {tab.icon}
                    {tab.label}
                  </div>
                  <ChevronRight className={activeTab === tab.id ? 'opacity-100' : 'opacity-0'} />
                </button>
              ))}
            </div>
          </div>

          <div className="lg:col-span-7 bg-[#111] p-10 rounded-2xl border border-[#222]">
            <div className="flex flex-col md:flex-row justify-between items-start md:items-end mb-10 pb-6 border-b border-[#222]">
              <h3 className="text-3xl font-bold">{demographics[activeTab].title}</h3>
              <span className="text-[#6A1E2B] font-mono text-sm mt-2 md:mt-0 tracking-wider">PRJCT: LEGACY</span>
            </div>
            
            {demographics[activeTab].stats.map((stat, i) => (
              <StatBar 
                key={i} 
                percentage={stat.p} 
                label={stat.l} 
                description={stat.d} 
                isVisible={spectrumVisible} 
              />
            ))}
            
            <div className="mt-10 pt-6 border-t border-[#222]">
              <p className="text-sm uppercase tracking-widest text-[#555] mb-4 font-bold">Services</p>
              <div className="grid grid-cols-2 gap-4">
                {demographics[activeTab].services.map((service, i) => (
                  <div key={i} className="bg-[#1a1a1a] p-4 rounded-lg border border-[#333] text-[#B0B0B0] text-center hover:border-[#6A1E2B] transition-all">
                    {service}
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* SERVICE STRIP */}
      <section className="bg-[#6A1E2B] py-20 px-6">
        <div className="max-w-7xl mx-auto grid md:grid-cols-3 gap-12 text-center divide-y md:divide-y-0 md:divide-x divide-white/20">
          {[
            { title: "Education", text: "We teach the 'why' behind the movement.", icon: <BookOpen size={56} /> },
            { title: "Programming", text: "Evidence-based, calculated progression.", icon: <ClipboardCheck size={56} /> },
            { title: "Application", text: "High-level coaching and technique precision.", icon: <Target size={56} /> }
          ].map((item, i) => (
            <div key={i} className="px-4 py-4 md:py-0">
              <div className="mb-6 opacity-90 flex justify-center">{item.icon}</div>
              <h3 className="text-2xl font-bold mb-3">{item.title}</h3>
              <p className="text-white/80">{item.text}</p>
            </div>
          ))}
        </div>
      </section>

      {/* PROTOCOL SECTION */}
      <section 
        ref={protocolRef}
        className={`py-24 md:py-32 px-6 max-w-7xl mx-auto transition-all duration-1000 ${protocolVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'}`}
      >
        <div className="text-center mb-20">
          <h2 className="text-4xl md:text-5xl font-bold mb-6">The Legacy <span className="text-[#6A1E2B]">Protocol</span></h2>
          <p className="text-[#B0B0B0] text-lg max-w-2xl mx-auto">A systematic methodology for physical transformation.</p>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
          {["Assess", "Program", "Adapt", "Overcome"].map((step, i) => (
            <div key={i} className="p-8 rounded-xl bg-[#111] border border-[#222] hover:border-[#6A1E2B]/50 transition-all group">
              <div className="text-5xl font-black text-[#222] group-hover:text-[#6A1E2B]/20 mb-4 transition-colors">0{i+1}</div>
              <h4 className="text-xl font-bold mb-2">{step}</h4>
              <p className="text-[#666] text-sm">Targeted results through physiological precision.</p>
            </div>
          ))}
        </div>
      </section>

      {/* FOOTER */}
      <footer id="contact" className="border-t border-[#222] bg-[#050505] py-24 px-6">
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-5xl md:text-7xl font-black mb-8 tracking-tighter">
            READY TO BUILD YOUR <br />
            <span className="text-[#6A1E2B]">LEGACY?</span>
          </h2>
          <div className="grid md:grid-cols-2 gap-6 max-w-lg mx-auto mb-20">
            <button className="p-8 rounded-xl bg-[#1a1a1a] border border-[#333] hover:border-[#6A1E2B] transition-all text-left group">
              <Mail className="mb-4 text-[#6A1E2B]" />
              <h4 className="font-bold text-xl group-hover:text-[#6A1E2B]">Referrals</h4>
              <p className="text-[#666] text-sm">B2B & Clinical Pathways</p>
            </button>
            <button className="p-8 rounded-xl bg-[#1a1a1a] border border-[#333] hover:border-[#6A1E2B] transition-all text-left group">
              <Target className="mb-4 text-[#6A1E2B]" />
              <h4 className="font-bold text-xl group-hover:text-[#6A1E2B]">Consultations</h4>
              <p className="text-[#666] text-sm">Book Initial Assessment</p>
            </button>
          </div>
          <div className="pt-8 border-t border-[#1a1a1a] flex flex-col md:flex-row justify-between items-center text-[#444] text-sm font-medium">
            <p>© 2024 PRJCT: Legacy</p>
            <div className="flex gap-8 mt-4 md:mt-0">
              <Instagram size={20} className="hover:text-white cursor-pointer" />
              <Linkedin size={20} className="hover:text-white cursor-pointer" />
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
}
